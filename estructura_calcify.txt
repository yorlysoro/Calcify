# Calcify — Reporte de Estado del Proyecto

## 1. Resumen General

Calcify es una aplicación de escritorio para gestión de inventario y
divisas (currency/inventory management) construida con Flask 3 +
SQLAlchemy 2.0 bajo Clean Architecture (4 capas estrictas).

- **Versión actual:** 0.9.1 (en desarrollo activo)
- **Python:** 3.13+
- **Framework web:** Flask 3.1.3
- **ORM:** SQLAlchemy 2.0.49
- **Base de datos:** SQLite (con migraciones automáticas vía Alembic 1.18.4)
- **Internacionalización:** Flask-Babel 4.0 (Español e Inglés, detección automática + botón manual)
- **Backend tests:** Pytest 9.0.3 (142 tests)
- **Frontend tests:** Jest 29 + jsdom (80 tests)
- **CSS:** Tailwind CSS 3.4 (build local vía npm)
- **Cobertura backend:** 90% (1021/1138 statements)

## 2. Estructura de Directorios

```
Calcify/
├── app.py                          # Application Factory + i18n (Babel, locale selector, context processor)
├── babel.cfg                       # Configuración de extractores Babel (Python + Jinja2)
├── run_coverage.py                 # Script de cobertura (cross-platform)
├── setup_security.py               # Bootstrap de seguridad (admin PIN + secret key)
├── reset_password.py               # CLI interactivo para reset de PIN
├── scaffold.py                     # Generador de estructura inicial
├── requirements.txt                # Dependencias (22 paquetes, incluye Flask-Babel + Babel)
├── AGENTS.md                       # Guía de arquitectura para asistentes IA
├── changelog.md                    # Historial de cambios (Keep a Changelog)
├── package.json                    # Frontend: npm scripts (build:css, test)
├── tailwind.config.js              # Tailwind content paths, fonts, cyber theme
├── scripts/
│   └── update_translations.sh      # Automatización: extraer → actualizar → compilar traducciones
├── domain/                         # CAPA 1: Domain (0 dependencias externas)
│   ├── models.py                   #   Entidades: Currency, ExchangeRate, Product,
│   │                               #              CurrencyRate, Transaction
│   ├── exceptions.py               #   InvalidExchangeRateError
│   └── services/
│       ├── __init__.py             #   CurrencyConverter (instance method con ExchangeRate)
│       └── currency_converter.py   #   CurrencyConverter.convert() (inverse-rate, Decimal)
├── use_cases/                      # CAPA 2: Use Cases (solo interfaces abstractas)
│   ├── sales.py                    #   RegisterSaleUseCase
│   ├── currency_conversion.py      #   CurrencyConversionUseCase
│   └── export_backup.py            #   ExportBackupUseCase
├── infrastructure/                 # CAPA 3: Infrastructure (SQLAlchemy, Alembic)
│   ├── database/
│   │   ├── models.py               #   ORM models (5 tablas)
│   │   ├── session.py              #   get_db_path() — path OS-agnóstico
│   │   ├── auto_migrate.py         #   bootstrap_migrations() — migración automática
│   │   └── migrations.py           #   run_migrations() — CLI alternativo
│   └── repositories/
│       ├── interfaces.py           #   5 interfaces abstractas (ABC)
│       └── sqlalchemy_repos.py     #   Implementaciones concretas con mapeo a dominio
├── presentation/                   # CAPA 4: Presentation (Flask Blueprints)
│   ├── __init__.py                 #   Package marker
│   ├── api/
│   │   ├── auth.py                 #   auth_bp: /login, /logout, @login_required (strings i18n)
│   │   └── routes.py               #   api_bp: 16 endpoints REST + 2 locale endpoints (strings i18n)
│   ├── web/
│   │   ├── __init__.py             #   Package marker
│   │   └── routes.py               #   web_bp: / (index), /login (GET)
│   └── templates/
│       ├── base.html               #   Template base con bloques (lang dinámico, locale_data, title, content, scripts)
│       ├── index.html              #   {% extends "base.html" %} — SPA con textos traducibles + botón EN/ES
│       └── login.html              #   {% extends "base.html" %} — Login con textos traducibles
├── translations/                   # Catálogos de traducción Babel
│   └── es/
│       └── LC_MESSAGES/
│           ├── messages.po         #   Traducciones al español (200+ cadenas)
│           └── messages.mo         #   Compilado binario
├── static/                         # Frontend assets (Tailwind local build)
│   ├── src/
│   │   └── input.css               #   Entrada Tailwind: directivas + custom overrides
│   ├── dist/
│   │   └── tailwind.css            #   Build minificado de Tailwind
│   ├── css/                        #   CSS por vista (cargados en base.html)
│   │   ├── base.css                #     Estilos base, layout, cyber background
│   │   ├── login.css               #     Estilos específicos del login
│   │   ├── calculator.css          #     Estilos de la calculadora de divisas
│   │   ├── inventory.css           #     Estilos del inventario de productos
│   │   ├── config.css              #     Estilos de configuración (monedas, tasas)
│   │   ├── reports.css             #     Estilos del reporte de transacciones
│   │   └── sales.css               #     Estilos del registro de ventas
│   └── js/                         #   JS plano (11 archivos, sin módulos)
│       ├── i18n.js                 #     __(key), __locale — función de traducción global
│       ├── utils.js                #     formatMoney (locale-aware), truncate, escapeHtml, formatDate, getBaseCurrency, getInverseRate
│       ├── components.js           #     showMessage, loadingIndicator, renderCurrencySelect, refreshApp
│       ├── api-client.js           #     ApiClient.get/post/put/delete con error handling y 401 redirect
│       ├── app.js                  #     App: init/bootstrap con Promise.all, status Online/Offline traducible
│       ├── login.js                #     LoginView: submit por PIN via ApiClient.post, textos traducibles
│       ├── calculator.js           #     CalculatorView: init con selects, render de conversiones multi-moneda
│       ├── inventory.js            #     InventoryView: CRUD con modals, filtro, textos traducibles
│       ├── config.js               #     ConfigView: gestión de monedas/tasas, textos traducibles
│       ├── reports.js              #     ReportView: filtro por fecha, tabla, CSV export, textos traducibles
│       └── sales.js                #     SalesView: form de venta, textos traducibles
├── tests/                          # 142 tests backend + 80 tests frontend
│   ├── conftest.py                 #   Fixtures: db_engine, db_session, client
│   ├── domain/
│   │   ├── test_models.py          #   1 test (Product entity)
│   │   └── test_currency_converter.py  # 12 tests (CurrencyConverter)
│   ├── use_cases/
│   │   ├── test_sales.py           #   9 tests (RegisterSaleUseCase)
│   │   └── test_currency_conversion.py # 10 tests (CurrencyConversionUseCase)
│   ├── infrastructure/
│   │   ├── test_auto_migrate.py    #   9 tests (bootstrap_migrations)
│   │   ├── test_migrations.py      #   2 tests (run_migrations)
│   │   ├── test_session.py         #   5 tests (platform paths)
│   │   ├── test_repositories.py    #   3 tests (repositorios)
│   │   ├── test_config_repository.py # 3 tests (config repo)
│   │   └── test_transaction_repository.py # 3 tests (transaction repo)
│   ├── presentation/
│   │   ├── test_app.py             #  10 tests (app factory, i18n)
│   │   └── test_routes.py          #  65 tests (endpoints API)
│   ├── test_currency_converter.py  #   5 tests (legacy converter)
│   ├── test_product.py             #   4 tests (Product domain)
│   └── frontend/                   # Tests Jest con jsdom (78 tests)
│       ├── jest.config.js          #   Config: jsdom, rootDir, setupFiles, moduleNameMapper (CSS mock)
│       ├── setup.js                #   Mock de localStorage, fix de console.error, mocks i18n (__(), _t, __locale)
│       ├── __mocks__/
│       │   └── styleMock.js        #   Mock vacío para imports de CSS en tests
│       ├── utils.test.js           #   formatMoney, truncate, escapeHtml, getInverseRate, getBaseCurrency
│       ├── api-client.test.js      #   ApiClient GET/POST/PUT/DELETE, errores HTTP, network failure
│       ├── login.test.js           #   LoginView: submit exitoso, PIN incorrecto, campos vacíos
│       ├── calculator.test.js      #   CalculatorView: init, render, moneda base faltante, tasas ausentes
│       ├── inventory.test.js       #   InventoryView: render, CRUD modals, stock bajo, filtro, API error
│       ├── config.test.js          #   ConfigView: render monedas/tasas, badge Main, botón Set Base
│       ├── reports.test.js         #   ReportsView: init con fecha, render tabla, export CSV, API error
│       ├── sales.test.js           #   SalesView: init selects, filterProducts, placeholder, submit error
│       └── app.test.js             #   AppView: bootstrap con API failure, console.error en red
├── iniciar_debian.sh               # Launcher Linux
└── instalar_y_correr.bat           # Launcher Windows (mensajes en inglés)
```

## 3. Capas de Arquitectura (Clean Architecture)

Reglas estrictas de dependencia (una vía, de afuera hacia adentro):

| Capa | Depende de | Imports prohibidos |
|------|-----------|-------------------|
| `domain/` | Python stdlib | Flask, SQLAlchemy, cualquier framework |
| `use_cases/` | Entidades domain + interfaces abstractas | ORM models, Flask |
| `infrastructure/` | SQLAlchemy, entidades domain, interfaces | Flask globals (request, g, session) |
| `presentation/` | Flask, entidades domain, repos concretos | ORM models directamente |

## 4. Entidades del Dominio (domain/models.py)

### Currency
- code: str (PK, ISO 4217, ej. "USD")
- name: str
- symbol: str
- is_main: bool

### ExchangeRate (legacy)
- base_currency_code: str
- target_currency_code: str
- rate: Decimal
- date: datetime

### Product
- id: UUID (PK)
- name: str
- cost_price: Decimal
- cost_currency_code: str (FK → currencies.code)
- margin_percentage: Decimal
- category: str = "Uncategorized"
- stock_quantity: int = 0
- calculate_sale_price() → Decimal (cost_price * (1 + margin/100))
- get_sale_price_in_currency(...) → Decimal (convierte a otra moneda)

### CurrencyRate
- id: UUID (PK)
- currency_code: str
- rate: Decimal
- inverse_rate: Decimal = 0.0
- created_at: datetime (timezone-aware)

### Transaction
- id: UUID (PK)
- product_id: UUID (FK → products.id)
- transaction_type: str ("IN" | "OUT")
- quantity: int
- unit_price: Decimal
- currency_code: str
- created_at: datetime (timezone-aware)
- comment: str = ""

## 5. Servicios del Dominio (domain/services/currency_converter.py)

### CurrencyConverter.convert(amount, source_inverse, target_inverse) → Decimal
- Fórmula: (amount * target_inverse) / source_inverse
- Quantize a 4 decimales con ROUND_HALF_UP
- Lanza InvalidExchangeRateError si algún inverse_rate es 0

## 6. Casos de Uso (use_cases/)

| Use Case | Método | Descripción |
|----------|--------|-------------|
| RegisterSaleUseCase | execute(product_id, quantity, unit_price, currency_code, comment) | Valida stock > quantity, crea Transaction OUT, reduce stock, retorna SaleResult |
| CurrencyConversionUseCase | execute(source_code, target_code, amount) | Convierte moneda a través de la moneda base usando inverse_rates |
| ExportBackupUseCase | execute() | Exporta todos los datos (currencies, products, transactions) como dict serializable |

## 7. API REST (presentation/api/routes.py) — 18 endpoints

| Método | Ruta | Función | Propósito |
|--------|------|---------|-----------|
| GET | /api/v1/currencies | get_currencies | Lista todas las monedas |
| POST | /api/v1/currencies | create_currency | Crea moneda (detecta duplicados → 409) |
| POST | /api/v1/products | create_product | Crea producto con validación |
| GET | /api/v1/products/<id> | get_product | Obtiene producto por ID con precio calculado |
| PUT | /api/v1/products/<id> | update_product | Actualiza campos parciales del producto |
| GET | /api/v1/products | get_products | Lista todos los productos |
| POST | /api/v1/transactions | create_transaction | Crea transacción IN/OUT |
| GET | /api/v1/transactions | get_transactions | Lista transacciones (filtro ?date=YYYY-MM-DD) |
| POST | /api/v1/rates | create_currency_rate | Crea tasa de cambio |
| GET | /api/v1/rates/latest | get_latest_rates | Última tasa por moneda |
| DELETE | /api/v1/rates/<id> | delete_currency_rate | Elimina tasa por ID |
| POST | /api/v1/convert | convert_currency | Convierte monto entre monedas |
| POST | /api/v1/sales | register_sale | Registra venta (reduce stock + crea OUT) |
| PUT | /api/v1/currencies/<code>/set_main | set_main_currency | Designa moneda base |
| DELETE | /api/v1/products/<id> | delete_product | Elimina producto |
| GET | /api/v1/backup/export | export_backup | Exporta backup como JSON (descarga, nombre calcify_backup_) |
| **GET** | **/api/v1/locale/<locale>** | set_locale | Cambia idioma via URL y redirige |
| **POST** | **/api/v1/locale** | set_locale_json | Cambia idioma via JSON |

## 8. Autenticación (presentation/api/auth.py)

- POST /login — espera {"pin": "..."}, verifica contra hash en DB
- POST /logout — limpia sesión
- @login_required — decorator que retorna 401 (API) o redirect (web)
- Session-based auth (no JWT)
- Session fixation prevention: session.clear() antes de login exitoso
- CSRF no implementado (app de escritorio single-user)

## 9. Base de Datos (5 tablas SQLite)

| Tabla | Columnas clave |
|-------|---------------|
| currencies | code (PK String(3)), name, symbol, is_main |
| products | id (PK UUID), name, cost_price (Numeric(14,2)), cost_currency_code (FK), margin_percentage (Numeric(5,2)), stock_quantity, category |
| transactions | id (PK UUID), product_id (FK), transaction_type (String(3)), quantity, unit_price (Numeric(14,2)), currency_code, comment, created_at |
| currency_rates | id (PK UUID), currency_code, rate (Numeric(14,6)), inverse_rate (Numeric(24,12)), created_at |
| configurations | key (PK String(100)), value (Text), updated_at |
| alembic_version | version_num (PK) — gestión automática |

## 10. Migraciones Automáticas (infrastructure/database/auto_migrate.py)

bootstrap_migrations(db_url, metadata) ejecuta en cada boot:

1. Migra artefactos legacy (alembic.ini, migrations/, schema_trace.sql)
   del project root al AppData dir
2. Inicializa Alembic (alembic.ini + migrations/) si no existen
3. Parchea env.py con sys.path, target_metadata, render_as_batch
4. Detecta estado de la DB: fresh, tables_exist_no_tracking, o normal
5. ALTER TABLE repair para columnas faltantes
6. Detecta diferencias de schema (tipos, columnas faltantes/sobrantes)
7. Ejecuta comandos Alembic según el estado:
   - Fresh DB → revision(autogenerate=True) + upgrade
   - Sin tracking → revision(no autogenerate) + stamp
   - Diferencias no-aditivas → revision(autogenerate=True) + upgrade
   - Normal → upgrade("head")
8. Maneja CommandError: "No changes" (log), "Can't locate revision"
   (DROP alembic_version + nueva baseline + stamp), otros (re-raise)
9. Genera schema_trace.sql (DDL offline dump)

## 11. Internacionalización (i18n)

### Arquitectura
- **Backend:** Flask-Babel 4.0 con locale_selector que prioriza:
  1. `session["locale"]` — selección manual del usuario
  2. `request.accept_languages.best_match(["en", "es"])` — automático (navegador)
  3. `"en"` — fallback por defecto
- **Frontend:** Función global `__(key)` respaldada por objeto `_t` inyectado desde Flask via template
- **Catálogos:** Babel `.po`/`.mo` en `translations/es/LC_MESSAGES/`

### Locale selector (UI)
- Botón EN/ES en navbar del header (cambia `session["locale"]`)
- `GET /api/v1/locale/<locale>` — redirect a referrer
- `POST /api/v1/locale` — JSON `{"locale": "es"}`

### Strings traducibles
- Templates Jinja2: `{{ _("Calculator") }}`, `{{ _("Sales") }}`, etc.
- Python API: `_("Invalid or missing JSON payload.")` con soporte `%(var)s`
- JavaScript: `__("register_sale")`, `__("no_products")`, etc.

### Cobertura
- ~200 cadenas traducidas al español (backend + frontend + templates)
- 100% de strings de usuario envueltos en `_()` o `__()`
- `formatMoney()` usa `__locale` (locale-aware Intl.NumberFormat)

### Actualizar traducciones
```bash
bash scripts/update_translations.sh
```

## 12. Frontend Architecture

### CSS (7 archivos, carga secuencial en base.html)

| Archivo | Propósito |
|---------|-----------|
| `static/css/base.css` | Variables, layout general, fondo cyberpunk animado, tipografía, scrollbar |
| `static/css/login.css` | Login card centrada, input neon, botón glitch |
| `static/css/calculator.css` | Grid de resultados por moneda, estilos de conversión |
| `static/css/inventory.css` | Tabla de productos, badges de stock, modals CRUD |
| `static/css/config.css` | Currency cards, rates list, botón de export |
| `static/css/reports.css` | Tabla de transacciones, resumen de tarjetas |
| `static/css/sales.css` | Formulario de venta, search de productos |

### JS (11 archivos, plano sin módulos ES)

Cargados en `base.html` en orden de dependencia (i18n → utils → components → api-client → app → vistas).
No usan `import`/`export` porque los templates HTML referencian funciones globales via `onclick`.

| Archivo | API pública | Dependencias |
|---------|-------------|-------------|
| `i18n.js` | `__(key)`, `__locale` | — |
| `utils.js` | `formatMoney()` (locale-aware), `truncate()`, `escapeHtml()`, `formatDate()`, `getBaseCurrency()`, `getInverseRate()` | i18n |
| `components.js` | `showMessage()`, `loadingIndicator()`, `renderCurrencySelect()`, `refreshApp()` | utils |
| `api-client.js` | `ApiClient.get()`, `.post()`, `.put()`, `.delete()` | — |
| `app.js` | `App.init()`, `App.state` (currencies, products, rates, transactions) | utils, components, api-client |
| `login.js` | `LoginView.init()` | utils, api-client |
| `calculator.js` | `CalculatorView.init()`, `CalculatorView.render()` | utils, api-client |
| `inventory.js` | `InventoryView.init()`, `.render()`, `.openCreateModal()`, `.editProduct()`, `.deleteProduct()` | utils, components, api-client |
| `config.js` | `ConfigView.init()`, `.renderCurrencies()`, `.renderRates()` | utils, components, api-client |
| `reports.js` | `ReportView.init()`, `.fetchAndRender()`, `.exportCSV()` | utils, components, api-client |
| `sales.js` | `SalesView.init()`, `.filterProducts()`, `.handleSubmit()` | utils, components, api-client |

### Tailwind CSS

- Build: `npm run build:css` → `npx tailwindcss -i ./static/src/input.css -o ./static/dist/tailwind.css --minify`
- Content scan: `presentation/templates/**/*.html` (purgado de clases no usadas)
- Tema cyberpunk: colores personalizados (cyber-*), fuente Orbitron + Fira Code

### Frontend Tests (Jest + jsdom)

- 78 tests, 9 suites, sin dependencias externas (mocks manuales de fetch/DOM)
- Patrón `(0, eval)(code)` para cargar scripts planos en scope global de Jest
- Setup: `localStorage` mock, `console.error` preservado, CSS mock (`styleMock.js`), **mocks i18n** (`__()`, `_t`, `__locale`)
- Cobertura defensiva: estados vacíos, errores HTTP (401, 500), network failure, NaN, negativos, caracteres especiales, elementos DOM faltantes, placeholders sin resultados

```bash
npm test
```

## 13. Patrones de Diseño Implementados

| # | Patrón | Ubicación |
|---|--------|-----------|
| 1 | Clean Architecture (4 capas) | Toda la estructura de paquetes |
| 2 | Application Factory | app.py → create_app(config_name) |
| 3 | Repository | interfaces.py (abstracto) → sqlalchemy_repos.py (concreto) |
| 4 | Dependency Injection | Repos inyectan Session; Use Cases inyectan interfaces de repo |
| 5 | Unit of Work | antes_request inyecta g.db_session; routes llaman commit(); teardown cierra |
| 6 | Decorator (Auth) | @login_required en presentation/api/auth.py |
| 7 | Adapter (ORM→Domain) | sqlalchemy_repos._map_to_domain() |
| 8 | Strategy (Migration) | auto_migrate (bootstrap) vs migrations (CLI alternativo) |
| 9 | Use Case / Interactor | 3 use cases con método execute() |
| 10 | Domain Service (Stateless) | CurrencyConverter.convert() |
| 11 | SAVEPOINT Test Isolation | conftest.py — transacciones anidadas |
| 12 | Factory Method (Session) | Fixture db_engine con StaticPool |
| 13 | Upsert (Merge) | session.merge() para currencies y config |
| 14 | Lookup-Then-Update | Productos y transacciones: query + update explícito |
| **15** | **i18n (Flask-Babel + JS global)** | **app.py (locale_selector, context_processor), i18n.js (función __()), .po/.mo** |
| 16 | Structural Test (Type Inspection) | test_currency_converter.test_no_float_type_hints_in_domain_services — inspect.signature en el dominio |
| 17 | Centralized HTTP Layer (ApiClient) | login.js, inventory.js, config.js — todos migrados a ApiClient público (no raw fetch ni _request) |

## 14. Tests (222 tests: 142 backend + 80 frontend)

### Backend Fixtures (conftest.py)
- db_engine (session scope): SQLite in-memory con StaticPool
- db_session (function scope): SAVEPOINTs para aislamiento total
- client (function scope): create_app("testing") + test client

### Frontend Fixtures (setup.js + jest.config.js)
- jsdom environment para simular DOM del navegador
- localStorage mock (getItem/setItem/removeItem/clear)
- CSS mock (styleMock.js) para evitar errores de import
- **i18n mocks: `global.__()`, `global._t`, `global.__locale`**
- Patrón `(0, eval)(code)` para cargar JS plano en scope global

### Distribución

| Archivo | Tests | Cobertura que aporta |
|---------|-------|---------------------|
| tests/domain/test_currency_converter.py | 13 | domain/services/ (100%) |
| tests/domain/test_models.py | 1 | domain/models.py (91%) |
| tests/use_cases/test_sales.py | 9 | use_cases/sales.py (100%) |
| tests/use_cases/test_currency_conversion.py | 10 | use_cases/currency_conversion.py (95%) |
| tests/infrastructure/test_auto_migrate.py | 9 | auto_migrate.py (81%) |
| tests/infrastructure/test_migrations.py | 2 | migrations.py (93%) |
| tests/infrastructure/test_session.py | 5 | session.py (100%) |
| tests/infrastructure/test_repositories.py | 3 | sqlalchemy_repos.py (93%) |
| tests/infrastructure/test_config_repository.py | 3 | config repo (100%) |
| tests/infrastructure/test_transaction_repository.py | 3 | transaction repo (implícito) |
| tests/presentation/test_app.py | 10 | app.py (81%), i18n (locale selector) |
| tests/presentation/test_routes.py | 65 | routes.py (95%), auth.py (87%) |
| tests/test_currency_converter.py | 5 | legacy converter (100%) |
| tests/test_product.py | 4 | domain/models.py (91%) |
| tests/frontend/utils.test.js | 16 | formatMoney, truncate, escapeHtml, formatDate, getBaseCurrency, getInverseRate |
| tests/frontend/api-client.test.js | 8 | ApiClient.get/post/put/delete, HTTP errors, network failure |
| tests/frontend/login.test.js           | 5 | LoginView via ApiClient.post, errores, empty PIN, network error |
| tests/frontend/calculator.test.js | 6 | CalculatorView init, render, moneda base faltante, tasas ausentes |
| tests/frontend/inventory.test.js       | 13 | InventoryView render, CRUD modals, stock bajo, API error, ApiClient.put |
| tests/frontend/config.test.js          | 9 | ConfigView render monedas/tasas, Main badge, Set Base, empty states, ApiClient.put |
| tests/frontend/reports.test.js | 8 | ReportsView init, render tabla, export CSV, API failure |
| tests/frontend/sales.test.js | 7 | SalesView init, filterProducts, placeholder, submit error |
| tests/frontend/app.test.js | 8 | AppView bootstrap con API failure, console.error en red |

### Cobertura backend por archivo (90% total)

| Archivo | Statements | Cobertura |
|---------|-----------|-----------|
| app.py | 118 | 81% |
| domain/__init__.py | 0 | 100% |
| domain/exceptions.py | 2 | 100% |
| domain/models.py | 67 | 91% |
| domain/services/__init__.py | 16 | 100% |
| domain/services/currency_converter.py | 11 | 100% |
| infrastructure/database/__init__.py | 0 | 100% |
| infrastructure/database/auto_migrate.py | 155 | 81% |
| infrastructure/database/migrations.py | 28 | 93% |
| infrastructure/database/models.py | 44 | 100% |
| infrastructure/database/session.py | 20 | 100% |
| infrastructure/repositories/__init__.py | 0 | 100% |
| infrastructure/repositories/interfaces.py | 63 | 71% |
| infrastructure/repositories/sqlalchemy_repos.py | 118 | 93% |
| presentation/__init__.py | 0 | 100% |
| presentation/api/__init__.py | 0 | 100% |
| presentation/api/auth.py | 45 | 87% |
| presentation/api/routes.py | 345 | 95% |
| presentation/web/__init__.py | 0 | 100% |
| presentation/web/routes.py | 10 | 80% |
| use_cases/__init__.py | 0 | 100% |
| use_cases/currency_conversion.py | 41 | 95% |
| use_cases/export_backup.py | 22 | 100% |
| use_cases/sales.py | 28 | 100% |
| **TOTAL** | **1133** | **90%** |

### Cómo ejecutar

```bash
# Backend tests
python -m pytest

# Backend tests + cobertura
python run_coverage.py
# Equivalente directo:
python -m pytest --cov=domain --cov=use_cases --cov=infrastructure \
       --cov=presentation --cov=app --cov-report=term --cov-report=html tests/

# Frontend tests
npm test

# Build Tailwind CSS
npm run build:css

# Actualizar traducciones
bash scripts/update_translations.sh
```

## 15. Dependencias (requirements.txt + package.json)

### requirements.txt (backend)

alembic==1.18.4, **Babel>=2.14.0**, blinker==1.9.0, bottle==0.13.4,
click==8.4.1, Flask==3.1.3, **Flask-Babel>=4.0.0**, greenlet==3.5.1,
iniconfig==2.3.0, itsdangerous==2.2.0, Jinja2==3.1.6, Mako==1.3.12,
MarkupSafe==3.0.3, packaging==26.2, pluggy==1.6.0, proxy_tools==0.1.0,
Pygments==2.20.0, pytest==9.0.3, pytest-cov==6.1.1, pywebview==6.2.1,
SQLAlchemy==2.0.49, typing_extensions==4.15.0, Werkzeug==3.1.8

### package.json (frontend dev)

```json
{
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "tailwindcss": "^3.4.17"
  },
  "scripts": {
    "build:css": "tailwindcss -i ./static/src/input.css -o ./static/dist/tailwind.css --minify",
    "test": "jest --config tests/frontend/jest.config.js tests/frontend/"
  }
}
```

## 16. Plataformas Soportadas

- Linux: iniciar_debian.sh (verifica Python/venv, instala deps, resuelve IP LAN)
- Windows: instalar_y_correr.bat (auto-instala Python si falta, elevación admin,
  lanzamiento automático de navegador, mensajes en inglés)
- macOS: no tiene launcher dedicado pero funciona con python app.py

## 17. Estado del Proyecto (2026-06-19)

### Funcionalidades completadas
- [x] CRUD completo de productos, monedas, tasas de cambio y transacciones
- [x] Conversión de divisas con precisión decimal (ROUND_HALF_UP)
- [x] Registro de ventas con validación de stock
- [x] Autenticación por PIN con hash (Werkzeug)
- [x] Migraciones automáticas de esquema (Alembic + ALTER TABLE fallback)
- [x] Exportación de backup JSON (nombre calcify_backup_)
- [x] Reporte de cobertura (90%) con pytest-cov
- [x] Recuperación de revisiones huérfanas de Alembic (Can't locate revision)
- [x] Paths OS-agnósticos (Windows %APPDATA%, Linux ~/.config, macOS ~/Library)
- [x] Filtro por fecha en transacciones
- [x] SPA con Tailwind CSS (gestión, calculadora, reportes, ventas)
- [x] Frontend modular: 11 JS + 7 CSS con template inheritance (base.html)
- [x] Tailwind CSS local build vía npm (no CDN)
- [x] 78 tests frontend con Jest + jsdom (cobertura defensiva completa)
- [x] Patrón (0, eval)(code) para testing de JS plano sin módulos
- [x] **Internacionalización: Flask-Babel + función __() global en JS**
- [x] **Selector de idioma automático (Accept-Language) + manual (botón EN/ES)**
- [x] **200+ cadenas traducidas al español (backend, templates, JS)**
- [x] **Archivos de launcher Windows en inglés**
- [x] **Nombre de backup cambiado a calcify_backup_**
- [x] **Eliminado código muerto con float en dominio (`convert_with_rates()` — v0.9.1)**
- [x] **Login migrado a `ApiClient.post` en vez de `fetch()` crudo (v0.9.1)**
- [x] **Inventory y Config migrados a `ApiClient.put` en vez de `_request()` privado (v0.9.1)**
- [x] **Test estructural de anotaciones float en dominio (v0.9.1)**

### Pendientes / Áreas con baja cobertura (COMPLETADAS en v0.9.0)
- [x] app.py (81%) — paths de producción, locale selector, non-HTTP error handler probados
- [x] migrations.py (93%) — run_migrations() con/sin alembic_ini
- [x] session.py (100%) — Windows/macOS/Linux platform branching
- [x] routes.py (95%) — 16 handlers de error 500 ejercitados
- [x] interfaces.py (71%) — aceptado como falso positivo (pass en abstractos)
- [x] CLI scripts — mantenidos en inglés por decisión de diseño
